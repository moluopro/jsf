import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web registrar used by Flutter's generated plugin registrant.
///
/// JSF's web runtime is implemented with conditional exports from
/// `package:jsf/jsf.dart`, so registration does not need to install method
/// channels or global state.
class JsfWeb {
  /// Registers the web plugin with Flutter.
  static void registerWith(Registrar registrar) {}
}
